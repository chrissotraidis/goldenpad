diff --git a/patches/externs.h b/patches/externs.h
--- a/patches/externs.h
+++ b/patches/externs.h
@@ -409,5 +409,6 @@ extern Mtxf dword_CODE_bss_80079E98;
 extern f32 g_SkyCloudOffset;
 extern struct player* g_CurrentPlayer;
+extern PLAYER_ID array_PLAYER_IDs[4];
 void bondviewApplyVertaTheta(void);
 PropRecord* chrGetEquippedWeaponProp(struct ChrRecord* chr, s32 hand);
 void chrlvFireWeaponRelated(struct ChrRecord* chr, s32 hand);
diff --git a/patches/patches.h b/patches/patches.h
--- a/patches/patches.h
+++ b/patches/patches.h
@@ -125,6 +125,8 @@ void goldenpad_recomp_fire_rate_player_sample(
     s32 playernum, s32 weapon, s32 ammo, s32 counter);
 void goldenpad_recomp_fire_rate_guard_sample(
     s32 item, s32 before, s32 after, u32 guardKey);
+s32 goldenpad_recomp_render_order_probe(
+    s32 first, s32 second, s32 third, s32 fourth);
 void recomp_crash(const char* err);
 
 #endif
diff --git a/patches/syms.ld b/patches/syms.ld
--- a/patches/syms.ld
+++ b/patches/syms.ld
@@ -80,2 +80,3 @@ goldenpad_recomp_consume_return_to_title = 0x8F00011C;
 goldenpad_recomp_fire_rate_player_sample = 0x8F000120;
 goldenpad_recomp_fire_rate_guard_sample = 0x8F000124;
+goldenpad_recomp_render_order_probe = 0x8F000128;
diff --git a/patches/workbench_theboy.c b/patches/workbench_theboy.c
--- a/patches/workbench_theboy.c
+++ b/patches/workbench_theboy.c
@@ -599,6 +599,8 @@ RECOMP_PATCH void bossMainloop(void) {
     s32 freeGfx;
     s32 mainTickElapsed;
     s32 rspReplyMsg;
+    s32 fixedRenderOrder;
+    PLAYER_ID savedRenderOrder[4];
 
     u32 unused_stackpadding_[56];
 
@@ -811,7 +813,23 @@ RECOMP_PATCH void bossMainloop(void) {
                                 }
                             }
 
+                            fixedRenderOrder = goldenpad_recomp_render_order_probe(
+                                array_PLAYER_IDs[0], array_PLAYER_IDs[1],
+                                array_PLAYER_IDs[2], array_PLAYER_IDs[3]);
+                            if (fixedRenderOrder) {
+                                for (i = 0; i < 4; i++) {
+                                    savedRenderOrder[i] = array_PLAYER_IDs[i];
+                                    array_PLAYER_IDs[i] = i;
+                                }
+                            }
+
                             gdl = lvlRender(gdl);
+
+                            if (fixedRenderOrder) {
+                                for (i = 0; i < 4; i++) {
+                                    array_PLAYER_IDs[i] = savedRenderOrder[i];
+                                }
+                            }
 
                             // Lets Visualise the Coverage Value used for Scilohete Anti-Ailising (edges)
                             // (done on the VI), also produces a cool looking linemode - providing AA is working.
